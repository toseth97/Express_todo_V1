<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Todo App</title>
    <link rel="stylesheet" href="css/styles.css">
</head>

<body>
    <div class="box" id="heading">
        <h1>
            <%= title %>
        </h1>
    </div>
    <div class="box">
        <% console.log(items) %>
            <% for (i in items){ %>
                <form action="/delete" method="post">
                    <div class="item">
                        <input type="checkbox" value=<%=items[i]._id %> name="checkbox" onChange="this.form.submit()" >
                        <input type="hidden" name="name" value=<%=route %> > </input>
                        <p>
                            <%= items[i].name %>
                        </p>
                    </div>
                </form>

                <%}%>
                    <form class="item" action=<%=route %> method="post">
                        <input type="text" name="newItem" placeholder="Add new item" autofocus required />
                        <button class="submit" type="submit">+</button>
                    </form>
    </div>



</body>

</html>